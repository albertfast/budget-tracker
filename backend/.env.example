# Environment
APP_ENV=dev

# Database Configuration
DATABASE_URL=sqlite:///./budget_tracker.db

# Security Configuration
SECRET_KEY=your-secret-key-change-in-production-use-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Plaid API Configuration (Get these from Plaid Dashboard)
PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_secret_key
PLAID_ENV=sandbox

# Encryption Key for sensitive data (Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your_encryption_key_for_sensitive_data

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=Budget Tracker API
